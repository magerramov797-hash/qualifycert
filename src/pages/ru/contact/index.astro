---
import BaseLayout from "../../../layouts/BaseLayout.astro";
const title = "Контакты";
const desc =
  "Отправьте запрос по ISO-сертификации, аудиту или обучению. Мы ответим и поделимся планом работ и стоимостью.";
---

<BaseLayout title={title} description={desc}>
  <section class="section">
    <div class="container">
      <div class="kicker">Свяжитесь с нами</div>
      <h1 class="h1" style="margin-top:12px;">{title}</h1>
      <p class="p" style="max-width:860px;">
        Напишите, какой стандарт/услуга вас интересует — уточним детали и отправим план работ,
        сроки и стоимость.
      </p>

      <!-- Layout: left info + right form (responsive without breaking) -->
      <div class="grid contact-layout" style="margin-top:22px;">
        <!-- Left -->
        <div class="card contact-left">
          <div style="font-weight:700; margin-bottom:10px;">Контактная информация</div>

          <div style="display:grid; gap:10px;">
            <div>
              <div style="font-size:13px; color: var(--muted);">Телефон/WhatsApp</div>
              <div style="font-weight:600;">+994 10 725 64 41</div>
            </div>

            <div>
              <div style="font-size:13px; color: var(--muted);">Email</div>
              <div style="font-weight:600;">info@qualifycert.com</div>
            </div>

            <div>
              <div style="font-size:13px; color: var(--muted);">Город</div>
              <div style="font-weight:600;">Баку, Азербайджан</div>
            </div>
          </div>

          <div style="margin-top:18px; border-top:1px solid var(--line); padding-top:14px;">
            <div style="font-weight:700; margin-bottom:8px;">Что можно уточнить</div>
            <ul class="p contact-list">
              <li>План подготовки к ISO и сроки</li>
              <li>Коммерческое предложение</li>
              <li>Подготовительный аудит (gap-анализ)</li>
              <li>Программа обучения для персонала</li>
            </ul>
          </div>

          <div class="grid grid-auto" style="margin-top:14px; justify-items:center;">
            <a class="btn" href="mailto:info@qualifycert.com" style="width:100%;">Написать на email</a>
            <a class="btn" href="tel:+994107256441" style="width:100%;">Позвонить</a>
          </div>
        </div>

        <!-- Right -->
        <div class="card contact-right">
          <div style="font-weight:700; margin-bottom:10px;">Отправить запрос</div>
<form 
  action="https://formspree.io/f/maqdezen" 
  method="POST"
>
  <!-- Hidden config -->
  <input type="hidden" name="_subject" value="Новая заявка — qualifycert.com" />
  <input type="hidden" name="_captcha" value="false" />
  <input type="hidden" name="_next" value="https://qualifycert.com/ru/contact/?success=true" />

  <!-- Honeypot anti-spam -->
  <input type="text" name="_gotcha" style="display:none" />

  <div class="grid grid-auto form-grid">
    <div>
      <label class="field-label" for="name">Имя</label>
      <input id="name" name="name" required class="field" placeholder="Ваше имя" />
    </div>

    <div>
      <label class="field-label" for="company">Компания</label>
      <input id="company" name="company" class="field" placeholder="Название компании" />
    </div>

    <div>
      <label class="field-label" for="phone">Телефон / WhatsApp</label>
      <input id="phone" name="phone" required class="field" placeholder="+994..." />
    </div>

    <div>
      <label class="field-label" for="email">Email</label>
      <input id="email" name="email" type="email" class="field" placeholder="email@company.com" />
    </div>

    <div class="span-2">
      <label class="field-label" for="topic">Интересует</label>
      <select id="topic" name="topic" class="field field-select">
        <option>ISO сертификация</option>
        <option>Аудит</option>
        <option>Обучение</option>
        <option>ISO 9001</option>
        <option>ISO 14001</option>
        <option>ISO 45001</option>
        <option>ISO 22000 / HACCP</option>
      </select>
    </div>

    <div class="span-2">
      <label class="field-label" for="message">Сообщение</label>
      <textarea
        id="message"
        name="message"
        rows="5"
        class="field"
        style="resize:vertical;"
        placeholder="Краткое описание: сфера, количество сотрудников, сроки..."
      ></textarea>
    </div>
  </div>

  <div class="grid grid-auto" style="margin-top:14px; justify-items:center; max-width:520px;">
    <button class="btn primary" type="submit" style="width:100%;">
      Отправить заявку
    </button>
    <a class="btn" href="/ru/services/" style="width:100%;">
      Посмотреть услуги
    </a>
  </div>

  <p class="p" style="font-size:13px; margin-top:12px;">
    Нажимая кнопку, вы соглашаетесь на обработку персональных данных.
  </p>
</form>

        </div>
      </div>
    </div>
  </section>

  <style is:global>
    /* Layout (left + right) */
    .contact-layout{
      grid-template-columns: repeat(12, minmax(0, 1fr));
      gap: 18px;
    }
    .contact-left{ grid-column: span 5; }
    .contact-right{ grid-column: span 7; }

    @media (max-width: 900px){
      .contact-left,
      .contact-right{
        grid-column: 1 / -1;
      }
    }

    /* Lists */
    .contact-list{
      margin: 0;
      padding-left: 18px;
      display: grid;
      gap: 6px;
      color: var(--muted);
    }

    /* Form fields */
    .form-grid{
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 14px;
      margin-top: 10px;
    }

    /* Full width blocks inside auto grid */
    .span-2{
      grid-column: 1 / -1;
    }

    .field-label{
      font-size: 13px;
      color: var(--muted);
      display: block;
      margin-bottom: 6px;
    }

    .field{
      width: 100%;
      padding: 12px;
      border-radius: 12px;
      border: 1px solid var(--line);
      outline: none;
      background: #fff;
      color: var(--text);
    }

    .field:focus{
      border-color: rgba(37,99,235,.35);
      box-shadow: 0 0 0 3px rgba(37,99,235,.12);
    }

    .field-select{
      appearance: none;
    }
  </style>
</BaseLayout>
